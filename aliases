alias g="git"
alias gt="git status"
alias gd="git diff"
alias gc="git commit -a"
alias gr="git reset"
alias gs="git status"
alias gst="git staged"
alias dbdo="bin/rails db:environment:set RAILS_ENV=test && bin/rails db:drop RAILS_ENV=test  && bin/rails db:create RAILS_ENV=test && bin/rails db:migrate RAILS_ENV=test "
alias tc="git diff --name-status master... | egrep -v 'fixtures/|factories' | grep spec | cut -d$'\t' -f 2 | xargs bundle exec rspec"

alias bi="bundle install"
alias be="bundle exec"
alias fkndie="killall -KILL ruby;killall -KILL rake;killall -KILL thin;redis-cli flushall"
alias fixtmux="pkill -SIGWINCH -f tmux"

alias la='ls -ahF'
alias ll='ls -alhF'

alias mdiff='mvim -c ":colorscheme vividchalk" -c ":set nomodified" -'

alias rpi="ssh rpi.local -l pi"
