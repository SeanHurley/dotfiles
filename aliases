alias cls="clear"

alias cd.="cd .."
alias cd2.="cd ../.."
alias cd3.="cd ../../.."
alias cd4.="cd ../../../.."
alias cd5.="cd ../../../../.."
alias cd6.="cd ../../../../../.."

alias g="git"
alias gt="git status"
alias gd="git diff"
alias gc="git commit -a"
alias gr="git reset"
alias dbdo="rake db:do_over"
alias dbtp="rake db:truncate db:populate_data"

alias bi="bundle install"
alias be="bundle exec"
alias boomstick="shotgun -o 0.0.0.0"
alias fkndie="killall -KILL ruby;killall -KILL rake;killall -KILL thin;redis-cli flushall"

alias lasttest='$(cat /tmp/last_ruby_focused_unit_test)'
alias debuglasttest='$(cat /tmp/last_ruby_focused_unit_test | sed "s/spec/spec --debugger/")'
alias r3spec='RAILS3=x bundle exec rspec'

alias matebash="mate ~/.bash_login"
alias mategit="mate ~/.gitconfig"

alias mysqlstart='sudo /opt/local/bin/mysqld_safe5'
alias mysqlstop='/opt/local/bin/mysqladmin5 -u root shutdown'

alias rmate="mate Rakefile cruise_config README TODO app config doc db lib public script spec test stories features examples"

alias sc="test -e script/rails && script/rails c || script/console"
alias ss="test -e script/rails && script/rails s || script/server"
alias ssm="test -e script/rails && bundle exec script/rails s mongrel || bundle exec script/server mongrel"
alias ccss="CACHE_CLASSES=1 script/server"
alias rc="rake_commit"
alias sourcebash="source ~/.bash_login"

alias textedit="open -a TextEdit"

alias la='ls -ahF'
alias ll='ls -alhF'

alias mdiff='mvim -c ":colorscheme vividchalk" -c ":set nomodified" -'

alias revof-sandbox="revof sandbox"
alias revof-production="revof www"
alias revof-qa="revof qa"
alias revof-qa2="revof qa2"
alias revof-all="
  echo -n 'production: '; revof-production;
  echo -n 'sandbox: '; revof-sandbox;
  echo -n 'qa: '; revof-qa;
  echo -n 'qa2: '; revof-qa2;"


alias qatunnel="ssh -C -N -p 51322 -L 55432:localhost:5432 $USER@184.73.254.47"
alias prodmysql="mysql -h 127.0.0.1 -P 53306 -u $USER -D gateway -p"
alias prodpsql="psql -U ${USER}_ro -h 10.10.10.43 gateway"

markdown () {
  ruby -rrubygems -e "require 'redcarpet'; puts Redcarpet::Markdown.new(Redcarpet::Render::HTML, :fenced_code_block => true).render(File.read('$1'))" > /tmp/markdown.html && open /tmp/markdown.html
}

pbpush () {
  pbpaste  | ssh $1 'pbcopy'
}

alias print256colors='(x=`tput op` y=`printf %80s`;for i in {0..256};do o=00$i;echo -e ${o:${#o}-3:3} `tput setaf $i;tput setab $i`${y// /=}$x;done)'

alias top='top -o cpu'

alias rpi="ssh rpi.local -l pi"
